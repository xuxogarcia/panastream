# PanaStream - Example environment (root reference)
# Copy server/env.example to server/.env and fill in. Never commit .env.
# See server/env.example for all variables (AWS, S3, CloudFront, MediaConvert, PANASTREAM_API_TOKEN).
